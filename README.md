# AlgoCanvas

A playground where every algorithm becomes an animation. See [PRD.md](PRD.md) for
scope and architecture.

## Stack
React + TypeScript + Vite, rendering to a `<canvas>` 2D context.

## Development
```
npm install
npm run dev
```
