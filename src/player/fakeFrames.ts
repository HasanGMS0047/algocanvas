// Placeholder step sequence used only to exercise the player mechanics
// (play/pause/step/scrub/speed) before any real algorithm is wired in.
export const fakeFrames: string[] = [
  'idle',
  'compare(0, 1)',
  'swap(0, 1)',
  'compare(1, 2)',
  'compare(2, 3)',
  'swap(2, 3)',
  'compare(0, 1)',
  'done',
]
